[phases.setup]
nixPkgs = ["php", "php82Extensions.mysqli", "php82Extensions.pdo", "php82Extensions.pdo_pgsql", "php82Extensions.gd", "php82Extensions.mbstring", "php82Extensions.curl", "php82Extensions.zip", "php82Extensions.xml", "php82Extensions.bcmath"]

[phases.install]
cmds = ["echo 'No external dependencies to install'"]

[phases.build]
cmds = ["echo 'Build phase completed'"]

[start]
cmd = "php -S 0.0.0.0:3000 app.php"
